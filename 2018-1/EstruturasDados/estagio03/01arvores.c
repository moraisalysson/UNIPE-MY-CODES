//1 - hierarqica (não é sequencial/não lienar)

//nó raiz, filho, subárvore, folha

//subárvores são as ligações abaixo das raizes
//o número de subárvores de um nó determina o "grau de saída"

//na árvore só dá para andar para baixo (o filho não sabe quem é o pai)

//folha tem grau == 0

//só existe um caminho da raiz para qualquer nó

//árvore cheia: todos os nós possuem a mesma quantidade de filhos, que são iguais ao grau máximo da árvore

// --- Exercício slide pág 22

//a) folhas: b, g, e, h, i
//b) a == 2, b == 0, c == 3, d == 2, e == 0, f == 1, g == 0, h == 0, i == 0
//c) grau 3
//d) b -> a; g -> d, c, a; i -> f, c, a
//e) d -> g, h
//f) nível: 2, altura: 2
//g) nível e altura: 0
//h) altura: 3

